<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CoreventApp.ViewModels"
             x:DataType="viewmodels:ProfileViewModel"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             x:Class="CoreventApp.Views.Profile"
             Title="Perfil"
             BackgroundColor="{StaticResource background_color}">

  <ScrollView>
    <VerticalStackLayout Padding="16,14,16,20"
                         Spacing="20">

      <!-- Header: Title + Avatar -->
      <VerticalStackLayout
        VerticalOptions="Center">
        <Label Text="Perfil"
               FontSize="20"
               FontAttributes="Bold"
               TextColor="{StaticResource primary_text_color}"
               VerticalOptions="Center"/>
      </VerticalStackLayout>

      <!-- Profile Card -->
      <Border Stroke="{StaticResource Gray100}"
              StrokeThickness="1"
              StrokeShape="RoundRectangle 20"
              BackgroundColor="{StaticResource White}"
              Padding="20">
        <VerticalStackLayout Spacing="8"
                             HorizontalOptions="Center">
          <Border WidthRequest="80"
                  HeightRequest="80"
                  StrokeThickness="0"
                  StrokeShape="RoundRectangle 40"
                  BackgroundColor="{StaticResource card_icon_bg}"
                  HorizontalOptions="Center">
            <Image Source="{Binding UserAvatar}"
                   Aspect="AspectFill"
                   WidthRequest="80"
                   HeightRequest="80"/>
          </Border>
          <Label Text="{Binding UserName}"
                 FontSize="18"
                 FontAttributes="Bold"
                 HorizontalTextAlignment="Center"
                 TextColor="{StaticResource primary_text_color}"/>
          <Label Text="{Binding UserEmail}"
                 FontSize="13"
                 HorizontalTextAlignment="Center"
                 TextColor="{StaticResource secondary_text_color}"/>
        </VerticalStackLayout>
      </Border>

      <!-- Menu Items -->
      <VerticalStackLayout Spacing="12">

        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding EditProfileCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino Person}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Dados Pessoais"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>

        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding PurchaseHistoryCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino TagCircleFill}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Histórico de Compra"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>

        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding FavoritesCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino Heart}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Meus Favoritos"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>

        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding ReviewsCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino Star}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Avaliações"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>

        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding SettingsCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino Gear}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Configurações"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>
      </VerticalStackLayout>

      <!-- Organizador Section -->
      <VerticalStackLayout Spacing="12">
        <Label Text="ORGANIZADOR"
               FontSize="12"
               FontAttributes="Bold"
               TextColor="{StaticResource secondary_text_color}"
               Margin="4,0,0,0"/>
        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding PanelOrganizerCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino RectangleGrid2x2}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Painel do Organizador"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>
      </VerticalStackLayout>

      <!-- Colaborador Section -->
      <VerticalStackLayout Spacing="12">
        <Label Text="COLABORADOR"
               FontSize="12"
               FontAttributes="Bold"
               TextColor="{StaticResource secondary_text_color}"
               Margin="4,0,0,0"/>
        <Border Padding="14"
                Stroke="{StaticResource Gray100}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                BackgroundColor="{StaticResource White}">
          <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding PanelCollaboratorCommand}"/>
          </Border.GestureRecognizers>
          <Grid ColumnDefinitions="Auto, *, Auto"
                ColumnSpacing="14">
            <Border BackgroundColor="{StaticResource card_icon_bg}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 10"
                    WidthRequest="36"
                    HeightRequest="36"
                    VerticalOptions="Center">
              <mi:MauiIcon Icon="{mi:Cupertino Shield}"
                           IconSize="18"
                           IconColor="{StaticResource secondary_text_color}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
            </Border>
            <Label Text="Painel do Colaborador"
                   Grid.Column="1"
                   VerticalOptions="Center"
                   FontSize="16"
                   TextColor="{StaticResource profile_menu_text}"/>
            <mi:MauiIcon Grid.Column="2"
                         Icon="{mi:Cupertino ChevronRight}"
                         IconSize="16"
                         VerticalOptions="Center"
                         IconColor="{StaticResource chevron_color}"/>
          </Grid>
        </Border>
      </VerticalStackLayout>

      <!-- Logout -->
      <Border Padding="14"
              Stroke="{StaticResource Gray100}"
              StrokeThickness="1"
              StrokeShape="RoundRectangle 20"
              BackgroundColor="{StaticResource White}">
        <Border.GestureRecognizers>
          <TapGestureRecognizer Command="{Binding LogoutCommand}"/>
        </Border.GestureRecognizers>
        <HorizontalStackLayout Spacing="10"
                               HorizontalOptions="Center">
          <mi:MauiIcon Icon="{mi:Cupertino SquareArrowDown}"
                       Rotation="-90"
                       IconSize="18"
                       IconColor="{StaticResource primary_orange_color}"
                       VerticalOptions="Center"/>
          <Label Text="Sair da Conta"
                 VerticalOptions="Center"
                 FontSize="16"
                 FontAttributes="Bold"
                 TextColor="{StaticResource primary_orange_color}"/>
        </HorizontalStackLayout>
      </Border>

    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
